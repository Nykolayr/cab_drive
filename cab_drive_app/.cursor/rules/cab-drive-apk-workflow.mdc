---
description: Cab Drive — APK/AAB только по явной просьбе пользователя
alwaysApply: true
---

# APK / AAB — только с ведома пользователя

## Жёсткий запрет

- **Не** собирать `apk` / `appbundle` / копировать в `D:\Temp` / предлагать «отдай заказчику», пока пользователь **явно** не попросил: «сделай апк», «сделай aab», «собери релиз» и т.п.
- После фикса бага / фичи — **сначала** `flutter run` (или что попросил проверить), **APK не делать сам**.
- Commit/push кода ≠ разрешение на APK. APK — отдельная явная команда.

## Пакеты по сторам (обязательно)

| Артефакт | Стор | `applicationId` | Как собрать |
|----------|------|-----------------|-------------|
| **AAB** | Google Play | **`com.cab.drive`** | `flutter build appbundle --release` (**без** rustore-флага) |
| **APK** | RuStore | **`com.appwawe.YDrive`** | `$env:CAB_DRIVE_RUSTORE='true'; flutter build apk --release` |

- Переключение в `android/app/build.gradle` по `CAB_DRIVE_RUSTORE` / `ORG_GRADLE_PROJECT_rustore` / `-Prustore`.
- «Сделай апк» / «для рустор» → **всегда** `CAB_DRIVE_RUSTORE=true`, проверить badging = `com.appwawe.YDrive`.
- «Сделай aab» / Play → **всегда** `com.cab.drive`, env rustore **сбросить**.
- `google-services.json` уже содержит оба `package_name`.

## Версии в `pubspec.yaml` (обязательно)

Формат: `1.1.{N}+{N}` — **patch и buildNumber всегда одинаковые**, без расхождений.

Примеры: `1.1.87+87` → следующий bump → `1.1.88+88`.

- Не делать `1.1.2+87` / `1.1.88+87` и т.п.
- При bump увеличивать **оба** числа на 1: `1.1.{N+1}+{N+1}`.

## Полный релиз (APK RuStore + AAB Play) — предпочтительно

0. **`powershell -File scripts/pre_apk_regression.ps1`** — exit 0.
1. Bump `1.1.{N}+{N}` (если не «без bump»).
2. **`powershell -File scripts/release_android.ps1`** — gate ключей, sync_env, gradle stop, APK+AAB, копии в `D:\Temp` (см. `cab-drive-release-build.mdc`).
3. Git commit/push — только если просил.

## Когда пользователь пишет только «сделай апк» (RuStore)

0. **`powershell -File scripts/pre_apk_regression.ps1`** — exit 0 (см. `docs/ARCHITECTURE_AND_FLOWS.md`, правило `cab-drive-pre-apk-regression.mdc`).
1. **Поднять версию** по схеме `1.1.{N}+{N}` (если не сказал «без bump» / «без поднятия»).
2. Либо полный `scripts/release_android.ps1`, либо вручную:
   - **`dart run tool/sync_env.dart`**
   - **`$env:CAB_DRIVE_RUSTORE='true'; flutter build apk --release`**
   - Проверить `aapt dump badging` → `package: name='com.appwawe.YDrive'`
   - **Скопировать** в `D:\Temp\cabdrive_{buildNumber}_rustore.apk`
   - Сбросить `$env:CAB_DRIVE_RUSTORE`
3. **Git:** commit + push — только если пользователь просил

## Когда пользователь пишет только «сделай aab» (Google Play)

0. **`powershell -File scripts/pre_apk_regression.ps1`** — exit 0.
1. Bump по схеме `1.1.{N}+{N}` (если не «без bump»).
2. Либо `scripts/release_android.ps1`, либо вручную:
   - `dart run tool/sync_env.dart`
   - Убедиться, что `CAB_DRIVE_RUSTORE` не задан
   - **`flutter build appbundle --release`**
   - Копировать в `D:\Temp\cabdrive_{buildNumber}.aab`

## Ключи

- Один файл **`.env`** в корне (шаблон **`.env.example`**).
- **Не** просить `--dart-define` для ключей.
- RuStore и Play — **один и тот же** upload-keystore (не плодить отдельный «только для RuStore»).

## Запрещено

- Сборка без `.env` на машине.
- APK «на всякий случай» после правок без запроса.
- Грузить в RuStore APK с `com.cab.drive` или в Play AAB с `com.appwawe.YDrive`.
- Пропускать bump / push / копирование в `D:\Temp`, когда пользователь **явно** заказал полный сценарий (кроме явного «без bump»).
